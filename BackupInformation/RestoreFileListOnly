----------------------------------------------------------------------------------------------------------------
--	RESTORE FILELISTONLY option allows you Returns a result set containing a list of the database and log files 
--  contained in the backup set 
----------------------------------------------------------------------------------------------------------------
RESTORE FILELISTONLY FROM DISK = [BackupAddress]
GO
